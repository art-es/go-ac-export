# Active Collab Export

## Installation

Copy configuration file 
```bash
cp config.json.example config.json
```

Fill `username` & `password` field for active collab exporting.  
Fill `spreadsheet_id` & `sheet_id` field for spreadsheet importing.

Running byte file
```bash
./ac-export
```
